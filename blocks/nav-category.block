	{.section collection}
		{.section categories}
		<nav class="navigation">
			<div class="{.if categoryFilter}{.or} active-link{.end}">
				<a href="{fullUrl}">All</a>
			</div>
			{.repeated section @}
			<div class="{.equal? categoryFilter @} active-link{.end}">
					<a href="{fullUrl}?category={@|url-encode}">{@}</a>
			</div>
			{.end}
		</nav>
		{.end}
	{.end}

